extends layout.pug

block content
    if name
        h2 Welcome #{name}
    else
        form(action="/hello" method="post")
            label(for="username") Please enter your name:  
                input(type="text" name="username")
            button(type="submit" value="Sumbit") Submit